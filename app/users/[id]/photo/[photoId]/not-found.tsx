import React from "react";

const NotFoundPage = () => {
  return (
    <div className="text-center font-semibold">
      The given User does not have a photo.
    </div>
  );
};

export default NotFoundPage;
